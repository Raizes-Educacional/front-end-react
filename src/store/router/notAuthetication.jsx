import { Outlet } from "react-router-dom";


// Outler armezenatation group de rotas

const NotAuthetication = () => {
    return (
        <main>
            <Outlet />
        </main>
    )
}

export default NotAuthetication;